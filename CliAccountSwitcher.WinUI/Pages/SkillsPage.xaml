<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="CliAccountSwitcher.WinUI.Pages.SkillsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CliAccountSwitcher.WinUI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:CliAccountSwitcher.WinUI.Models"
    DataContext="{x:Bind ViewModel}"
    Loaded="OnSkillsPageLoaded"
    Unloaded="OnSkillsPageUnloaded"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="SkillActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="MinWidth" Value="40" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="SkillsListSurfaceBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="8" />
            <Setter Property="Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
        <Style x:Key="SkillsListHeaderBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="12" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
        <Style x:Key="SkillsListItemContainerStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0,4" />
        </Style>
        <Style x:Key="SkillsListItemBorderStyle" TargetType="Border">
            <Setter Property="MinHeight" Value="80" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
    </Page.Resources>

    <Grid>
        <Frame IsEnabled="{x:Bind ViewModel.IsSkillsSupported, Mode=OneWay}">
            <Grid Padding="24" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock
                            x:Uid="SkillsPage_TitleTextBlock"
                            FontSize="28"
                            FontWeight="SemiBold" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind ViewModel.DescriptionText, Mode=OneWay}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button x:Name="RefreshSkillsButton" Click="OnRefreshSkillsButtonClicked">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontSize="14" Glyph="&#xE72C;" />
                                <TextBlock x:Uid="SkillsPage_RefreshSkillsTextBlock" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <AutoSuggestBox
                    x:Name="SkillSearchAutoSuggestBox"
                    x:Uid="SkillsPage_SkillSearchAutoSuggestBox"
                    Grid.Row="1"
                    QueryIcon="Find"
                    Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Grid Grid.Row="2" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{x:Bind ViewModel.SelectedSkillCountText, Mode=OneWay}" />

                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button
                            x:Name="DeleteSelectedSkillsButton"
                            Click="OnDeleteSelectedSkillsButtonClicked"
                            IsEnabled="{x:Bind ViewModel.HasSelectedSkills, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontSize="14" Glyph="&#xE74D;" />
                                <TextBlock x:Uid="SkillsPage_DeleteSelectedSkillsTextBlock" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ExportSkillsBackupButton" Click="OnExportSkillsBackupButtonClicked">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontSize="14" Glyph="&#xEDE1;" />
                                <TextBlock x:Uid="SkillsPage_ExportBackupTextBlock" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ImportSkillsBackupButton" Click="OnImportSkillsBackupButtonClicked">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontSize="14" Glyph="&#xE8B5;" />
                                <TextBlock x:Uid="SkillsPage_ImportBackupTextBlock" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="3" Style="{StaticResource SkillsListSurfaceBorderStyle}">
                    <Grid>
                        <ListView
                            x:Name="SkillsListView"
                            Background="Transparent"
                            ItemContainerStyle="{StaticResource SkillsListItemContainerStyle}"
                            ItemsSource="{x:Bind ViewModel.FilteredSkills, Mode=OneWay}"
                            SelectionMode="None">
                            <ListView.Header>
                                <Border Style="{StaticResource SkillsListHeaderBorderStyle}">
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="2.4*" />
                                            <ColumnDefinition Width="4.6*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1.5*" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox
                                            x:Name="SelectAllSkillsCheckBox"
                                            Grid.Column="0"
                                            MinWidth="0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Checked="OnSelectAllSkillsCheckBoxChecked"
                                            IsChecked="{x:Bind ViewModel.FilteredSkillsSelectionState, Mode=OneWay}"
                                            IsEnabled="{x:Bind ViewModel.HasFilteredSkills, Mode=OneWay}"
                                            IsThreeState="{x:Bind ViewModel.IsSelectAllSkillsCheckBoxThreeState, Mode=OneWay}"
                                            Unchecked="OnSelectAllSkillsCheckBoxUnchecked" />
                                        <TextBlock
                                            x:Uid="SkillsPage_SkillNameHeaderTextBlock"
                                            Grid.Column="1"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold" />
                                        <TextBlock
                                            x:Uid="SkillsPage_DescriptionHeaderTextBlock"
                                            Grid.Column="2"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold" />
                                        <TextBlock
                                            x:Uid="SkillsPage_FilesHeaderTextBlock"
                                            Grid.Column="3"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold" />
                                        <TextBlock
                                            x:Uid="SkillsPage_ModifiedHeaderTextBlock"
                                            Grid.Column="4"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold" />
                                    </Grid>
                                </Border>
                            </ListView.Header>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SkillItem">
                                    <Border Style="{StaticResource SkillsListItemBorderStyle}">
                                        <Grid ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="2.4*" />
                                                <ColumnDefinition Width="4.6*" />
                                                <ColumnDefinition Width="1*" />
                                                <ColumnDefinition Width="1.5*" />
                                            </Grid.ColumnDefinitions>

                                            <CheckBox
                                                Grid.Column="0"
                                                MinWidth="0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />

                                            <StackPanel
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Spacing="3">
                                                <TextBlock
                                                    FontSize="16"
                                                    FontWeight="SemiBold"
                                                    Text="{x:Bind Name, Mode=OneWay}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <TextBlock
                                                    FontFamily="Consolas"
                                                    FontSize="12"
                                                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                    Text="{x:Bind DirectoryName, Mode=OneWay}"
                                                    TextTrimming="CharacterEllipsis" />
                                            </StackPanel>

                                            <TextBlock
                                                Grid.Column="2"
                                                VerticalAlignment="Center"
                                                Text="{x:Bind Description, Mode=OneWay}"
                                                TextTrimming="CharacterEllipsis"
                                                TextWrapping="NoWrap">
                                                <ToolTipService.ToolTip>
                                                    <ToolTip>
                                                        <TextBlock
                                                            MaxWidth="520"
                                                            Text="{x:Bind Description, Mode=OneWay}"
                                                            TextWrapping="Wrap" />
                                                    </ToolTip>
                                                </ToolTipService.ToolTip>
                                            </TextBlock>

                                            <TextBlock
                                                Grid.Column="3"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Text="{x:Bind FileCount, Mode=OneWay}" />

                                            <TextBlock
                                                Grid.Column="4"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Text="{x:Bind LastModifiedText, Mode=OneWay}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="8"
                            Visibility="{x:Bind ViewModel.HasNoFilteredSkills, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <FontIcon
                                HorizontalAlignment="Center"
                                FontSize="36"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Glyph="&#xE943;" />
                            <TextBlock
                                x:Uid="SkillsPage_NoFilteredSkillsTitleTextBlock"
                                HorizontalAlignment="Center"
                                FontSize="18"
                                FontWeight="SemiBold" />
                            <TextBlock
                                x:Uid="SkillsPage_NoFilteredSkillsDescriptionTextBlock"
                                HorizontalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="8"
                            Visibility="{x:Bind ViewModel.HasNoSkills, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <FontIcon
                                HorizontalAlignment="Center"
                                FontSize="36"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Glyph="&#xE943;" />
                            <TextBlock
                                x:Uid="SkillsPage_NoSkillsTitleTextBlock"
                                HorizontalAlignment="Center"
                                FontSize="18"
                                FontWeight="SemiBold" />
                            <TextBlock
                                x:Uid="SkillsPage_NoSkillsDescriptionTextBlock"
                                HorizontalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Frame>

        <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" Visibility="{x:Bind ViewModel.IsZaiProviderSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel
                MaxWidth="440"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12">
                <FontIcon
                    HorizontalAlignment="Center"
                    FontSize="40"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Glyph="&#xE7BA;" />
                <TextBlock
                    x:Uid="SkillsPage_ZaiUnsupportedTitleTextBlock"
                    HorizontalAlignment="Center"
                    FontSize="20"
                    FontWeight="SemiBold"
                    TextAlignment="Center" />
                <TextBlock
                    x:Uid="SkillsPage_ZaiUnsupportedDescriptionTextBlock"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>

        <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" Visibility="{x:Bind ViewModel.IsOllamaProviderSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel
                MaxWidth="440"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12">
                <FontIcon
                    HorizontalAlignment="Center"
                    FontSize="40"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Glyph="&#xE7BA;" />
                <TextBlock
                    x:Uid="SkillsPage_OllamaUnsupportedTitleTextBlock"
                    HorizontalAlignment="Center"
                    FontSize="20"
                    FontWeight="SemiBold"
                    TextAlignment="Center" />
                <TextBlock
                    x:Uid="SkillsPage_OllamaUnsupportedDescriptionTextBlock"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
